version: '3'
services:
  compe-box:
    build: ./
    user: root
    volumes:
      - ./:/work
    working_dir: /work
    ports:
      - "8888:8888"
    environment:
      TZ: Asia/Tokyo
    tty: true
